name: Format and Test

on:
  push:
    branches: [ test ]
  pull_request:
    branches: [ test ]

jobs:
  test:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Format PowerShell scripts
        run: |
          powershell -Command "Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser"
          powershell -Command "Get-ChildItem -Path . -Filter *.ps1 -Recurse | ForEach-Object { Invoke-Formatter -Path $_.FullName -Verbose }"

      - name: Install Pester
        run: |
          powershell -Command "Install-Module -Name Pester -Force -Scope CurrentUser"

      - name: Run Pester tests
        run: |
          powershell -Command "Invoke-Pester -Path . -Output Detailed"

